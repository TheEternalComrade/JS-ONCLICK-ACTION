function doSomething(){
    document.getElementById("secretMessage").setAttribute("class", "")//gets the html with the id of secretMessage and sets the
    //the class attribute to nothing, meaning it no longer has a class of "hidden"
}

//something more interesting if we have time
/**function roll(){
    ranNum = Math.random()* 10
    document.getElementById("numDisplay").innerHTML = ranNum
}**/